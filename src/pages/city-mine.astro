---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getAllCityMinePosts, getAllCityMineTags } from "../lib/citymine";

const allPosts = await getAllCityMinePosts();
const allTags = await getAllCityMineTags();
---

<BaseLayout title="水紋都市鉱山| 砕けた水槽クラブ">
  <main class="wrapper posts-page">
    <h1 class="midashi">水紋都市鉱山</h1>

    <section class="filter-section">
      <div class="tag-filter">
        <button class="tag-btn active" data-tag="">すべて</button>
        {allTags.map((t) => (
          <button class="tag-btn" data-tag={t}>{t}</button>
        ))}
      </div>
    </section>

    <p class="post-count" id="post-count">{allPosts.length}件</p>

    <div class="card-grid" id="card-grid">
      {allPosts.map((post) => (
        <a href={"/city-mine/" + post.id + "/"} class="card" data-tags={JSON.stringify(post.tags ?? [])}>
          <div class="card-img">
            {post.image ? (
              <img src={post.image.url} alt={post.title} loading="lazy" />
            ) : (
              <div class="card-img-placeholder">📝</div>
            )}
          </div>
          <div class="card-body">
            <h2 class="card-title">{post.title}</h2>
            {post.description && <p class="card-desc">{post.description}</p>}
            {(post.tags ?? []).length > 0 && (
              <ul class="card-tags">
                {post.tags.map((t) => <li>{t}</li>)}
              </ul>
            )}
          </div>
        </a>
      ))}
    </div>

    <p class="no-posts" id="no-posts" style="display:none;">該当する記事がありません。</p>

    <nav class="pagination" id="pagination"></nav>
  </main>
</BaseLayout>

<script>
const POSTS_PER_PAGE = 10;
let currentTag = "";
let currentPage = 1;

const cards = Array.from(document.querySelectorAll("#card-grid .card"));
const noPosts = document.getElementById("no-posts");
const postCount = document.getElementById("post-count");
const pagination = document.getElementById("pagination");

function getFiltered() {
  return cards.filter((card) => {
    const tags = JSON.parse(card.dataset.tags ?? "[]");
    if (currentTag && !tags.includes(currentTag)) return false;
    return true;
  });
}

function render() {
  const filtered = getFiltered();
  const total = filtered.length;
  const totalPages = Math.max(1, Math.ceil(total / POSTS_PER_PAGE));
  currentPage = Math.min(currentPage, totalPages);
  const start = (currentPage - 1) * POSTS_PER_PAGE;
  const visible = filtered.slice(start, start + POSTS_PER_PAGE);

  cards.forEach((card) => (card.style.display = "none"));
  visible.forEach((card) => (card.style.display = "flex"));

  postCount.textContent = total + "件";
  noPosts.style.display = total === 0 ? "block" : "none";

  pagination.innerHTML = "";
  if (totalPages > 1) {
    if (currentPage > 1) {
      const btn = document.createElement("button");
      btn.className = "page-btn";
      btn.textContent = "← 前へ";
      btn.onclick = () => { currentPage--; render(); };
      pagination.appendChild(btn);
    }
    for (let i = 1; i <= totalPages; i++) {
      const btn = document.createElement("button");
      btn.className = "page-btn" + (i === currentPage ? " current" : "");
      btn.textContent = String(i);
      btn.onclick = () => { currentPage = i; render(); };
      pagination.appendChild(btn);
    }
    if (currentPage < totalPages) {
      const btn = document.createElement("button");
      btn.className = "page-btn";
      btn.textContent = "次へ →";
      btn.onclick = () => { currentPage++; render(); };
      pagination.appendChild(btn);
    }
  }
}

document.querySelectorAll(".tag-btn").forEach((btn) => {
  btn.addEventListener("click", () => {
    document.querySelectorAll(".tag-btn").forEach((b) => b.classList.remove("active"));
    btn.classList.add("active");
    currentTag = btn.dataset.tag ?? "";
    currentPage = 1;
    render();
  });
});

render();
</script>
